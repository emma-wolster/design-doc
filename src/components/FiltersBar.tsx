'use client';

export default function FiltersBar() {
  return (
    <div className="border-b border-slate-800 px-6 py-3 flex items-center gap-4 text-xs">
      <span className="text-slate-400">
        Filters will go here (topic, module, show-only toggle).
      </span>
    </div>
  );
}
